# Budget Reduction Tracking - Backend Environment Configuration
#
# This file serves as a template for environment variables required by the backend API.
# Copy this file to .env and update with your actual values.
#
# IMPORTANT:
# - NEVER commit .env files with real secrets to version control
# - Generate strong, unique secrets for production
# - Keep this file updated when adding new environment variables
#
# Setup:
#   cp .env.example .env
#   # Edit .env with your actual values
#   chmod 600 .env

# ============================================================================
# APPLICATION ENVIRONMENT
# ============================================================================

# Environment mode (development, production, test)
NODE_ENV=development

# Application port
PORT=3001

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Development:
DATABASE_URL=postgresql://budget_user:dev_password@localhost:5432/budget_tracking

# Production:
# DATABASE_URL=postgresql://budget_user:STRONG_PASSWORD@localhost:5432/budget_tracking

# ============================================================================
# JWT AUTHENTICATION
# ============================================================================

# JWT Access Token Secret
# CRITICAL: Use a strong random secret in production
# Generate: openssl rand -base64 64 | tr -d '\n'
JWT_SECRET=your-jwt-secret-change-in-production

# Access token expiration
# Examples: 15m, 30m, 1h
JWT_EXPIRES_IN=15m

# JWT Refresh Token Secret
# CRITICAL: Must be different from JWT_SECRET
# Generate: openssl rand -base64 64 | tr -d '\n'
REFRESH_TOKEN_SECRET=your-refresh-secret-change-in-production

# Refresh token expiration
# Examples: 7d, 14d, 30d
REFRESH_TOKEN_EXPIRES_IN=7d

# ============================================================================
# CORS CONFIGURATION
# ============================================================================

# Allowed CORS origins (comma-separated for multiple)
# Development: Frontend dev server
CORS_ORIGIN=http://localhost:5173

# Production: Your actual domain
# CORS_ORIGIN=https://budget-tracking.yourdomain.com

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Log level (error, warn, info, http, verbose, debug, silly)
LOG_LEVEL=info

# ============================================================================
# OPTIONAL SETTINGS
# ============================================================================

# Rate limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Bcrypt password hashing rounds
# BCRYPT_ROUNDS=12

# Application name
# APP_NAME=Budget Reduction Tracker

# Application URL
# APP_URL=http://localhost:5173
